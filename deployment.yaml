apiVersion: apps/v1
kind: Deployment
metadata:
  name: fastapi-app-service
  labels:
    app: fastapi-app-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: fastapi-app-service
  template:
    metadata:
      labels:
        app: fastapi-app-service
    spec:
      containers:
        - name: fastapi-app-service
          image: pearllakhs/fast_api:038fe3375a724038988037fbfbb12c9ada0db1b5
          ports:
            - containerPort: 80
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "250m"
              memory: "256Mi"

 