apiVersion: apps/v1
kind: Deployment
metadata:
  name: fastapi-app-service
  labels:
    app: fastapi-app-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: fastapi-app-service
  template:
    metadata:
      labels:
        app: fastapi-app-service
    spec:
      containers:
        - name: fastapi-app-service
          image: pearllakhs/fast_api:08f6e01c492ebdb03fef927d502d4a98cf8e7d7b
          ports:
            - containerPort: 80
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "250m"
              memory: "256Mi"

 