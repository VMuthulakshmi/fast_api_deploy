apiVersion: apps/v1
kind: Deployment
metadata:
  name: fastapi-app-service
  labels:
    app: fastapi-app-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: fastapi-app-service
  template:
    metadata:
      labels:
        app: fastapi-app-service
    spec:
      containers:
        - name: fastapi-app-service
          image: pearllakhs/fast_api:92ffb8e421040d2c0255765cb12519bcffbcfad1
          ports:
            - containerPort: 80
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "250m"
              memory: "256Mi"

 