apiVersion: v1
kind: Service
metadata:
  name: fastapi-app-service
  labels:
    app: fastapi-app-service
spec:
  type: LoadBalancer
  selector:
    app: fastapi-app-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80

 
 